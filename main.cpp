#include <iostream>
#include "loading.h"
#include <Windows.h>

using namespace std;

int main()
{
    // fast io
    cin.tie(NULL);

    loadingBar(); // loadingBar() nằm trong tiêu đề loading.h để tải hoạt ảnh.

    return 0;
}
